import React from 'react'
import ReactDOM from 'react-dom/client'
import CornHedgingTracker from '../corn-hedging-tracker'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CornHedgingTracker />
  </React.StrictMode>,
)
